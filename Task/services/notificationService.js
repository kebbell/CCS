// Notify merchant that their bank details have been updated
exports.notifyMerchant = async (merchantId) => {
    console.log(`Sending notification to merchant ${merchantId}`);
    // Mock notification logic (e.g., SMS or email)
    return Promise.resolve();
  };
  
